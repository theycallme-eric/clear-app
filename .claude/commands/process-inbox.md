Process files in the inbox folder and organize them into the correct locations.

## Instructions

1. Read `.claude/skills/inbox-workflow.md` for filing rules
2. Check `.claude/inbox/` for files
3. For each file:
   - Determine type (skill, session plan, agent, screenshot, spec, etc.)
   - Move to correct destination folder
   - Normalize filename if needed
4. Report what was moved
5. If any session plans were filed, ask if I should execute them

Do not auto-execute session plans. Always check in first.
